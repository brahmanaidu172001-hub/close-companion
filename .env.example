# Required for live AI analysis. The app gracefully falls back to a deterministic
# mock analyzer if this is missing, so the demo runs even without a key.
ANTHROPIC_API_KEY=sk-ant-...

# Optional. Defaults to claude-sonnet-4-6.
ANTHROPIC_MODEL=claude-sonnet-4-6
